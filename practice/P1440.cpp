#include <bits/stdc++.h> 
using namespace std;
const int maxn = 2e6 + 7;
int n, m, a[maxn];
priority_queue<int, greater<int>> q; 
int main() {
    ios_base ::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> n >> m;
    for (int i = 1; i <= n; ++i) {
        cin >> a[i]; 
    }
    for (int i = 1; i <= n; ++i) {
        
    }
    return 0; 
}